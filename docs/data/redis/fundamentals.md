---
title: Redis 持久化、复制与集群基础
description: Redis RDB、AOF、主从复制和集群基础。
---

# Redis 持久化、复制与集群基础

按数据可靠性与高可用的学习顺序，归并 Redis 持久化、复制和集群笔记。

## 持久化

### Redis持久化

- 支持两种持久化方式
  - RDB 方式
  - AOF 方式

> 以下命令均不区分大小写

#### RDB方式

- rdb方式的持久化是通过快照完成的

> 当符合一定条件时，Redis会自动将内存中的所有数据生成一份副本并保存到磁盘上，这个过程即为“快照”

##### 如何触发

分以下几种情况执行：

- 根据配置规则进行自动快照

  配置文件中已经预设了3个条件：

  ```
  save 900 1
  save 300 10
  save 60 10000
  ```

  每个快照条件占一行，以 save 开头，秒数 + 被修改的键（以上），条件之间是“或”的关系

- 用户执行 save 或 bgsave 命令

  手动执行快照，区别在于save是同步执行快照，当数据过多时，会阻塞服务器，导致Redis长时间不响应（生产环境尽量避免此操作），bgsave是以后台异步的方式执行快照操作，可以用 lastsave 查看最近一次快照执行成功的时间戳。

- 执行 `FLUSHALL` 命令

  `FLUSHALL` 会清空当前 Redis 实例中的所有数据库。是否触发额外的 RDB 持久化取决于 Redis 版本和配置，生产环境执行前必须确认目标实例和备份策略。

- 执行全量复制（full resynchronization）时，主实例可能生成 RDB 快照；部分重同步不一定触发新的快照



##### 快照原理

- 存储位置
  - dir：存储路径
  - dbfilename：文件名

- 过程
  1. 使用 Fork 函数复制一份当前进程（父进程）的副本（子进程）
  2. 父进程继续处理客户端发来的命令，子进程将内存中的数据写入硬盘
  3. 当子进程写完所有数据后会使用该临时文件替换旧的RDB文件，至此一次快照完成

> 执行 fork 时，操作系统会使用写时复制：父子进程先共享页面，父进程修改页面时才复制。内存通常不会立刻增加一倍，但快照期间写入越多，额外内存压力越大，可能触发 OOM。`vm.overcommit_memory=1` 只能改变内存分配策略，不能替代容量评估和监控。


#### AOF方式

- AOF（append only file）默认不开启，可以通过配置文件参数启用：appendonly yes
- 开启后会将每条会更改 Redis 的命令写到硬盘的 AOF 文件中
  - 位置和 RDB 文件一致
  - 文件名通过参数修改：appendfilename appendonly.aof

##### 实现方式

AOF文件以纯文本的形式记录了 Redis 执行的写命令（原始通信协议内容），每当达到一定条件时 Redis 就会自动重写 AOF 文件，以优化文件。

- auto-aof-rewrite-percentage 100：超过上次文件百分之多少重写
- auto-aof-rewrite-min-size 64mb：最小重写文件大小
- bgrewriteaof：手动执行重写

虽然每次执行更改数据库内容的操作都会写入 AOF 缓冲区，但真正落盘的时机由 `appendfsync` 和操作系统共同决定；`everysec` 通常最多允许约一秒的写入丢失，`no` 则完全交给操作系统，不能简单按“30 秒”估算。Redis 7 还支持多文件 AOF 格式，备份时应按版本文档操作。

- appendfsync always / everysec / no

always ：每次执行写入都同步（安全且最慢）

everysec：每秒一次同步

no：不主动执行同步操作（完全由操作系统控制，最快且最不安全）

> Redis 允许同时开启 AOF 和RDB，既保证了数据安全又使得进行备份等操作十分容易。此时重新启动 Redis 后 Redis 会使用 AOF 文件来恢复数据，因为 AOF 方式的持久化可能丢失的数据更少。

## 复制

### 复制

> 本质就是一主多从

#### 配置

- 配置文件添加：`replicaof 主数据库地址 主数据库端口`（旧版本也接受 `slaveof`）
- 运行时切换：`REPLICAOF 主数据库地址 主数据库端口`；停止复制使用 `REPLICAOF NO ONE`

获取相关复制信息：`info replication`
> 默认情况下副本是只读的；可通过 `replica-read-only no` 开启写入，但这会破坏主从数据一致性，通常不建议。

- `REPLICAOF NO ONE`：使当前实例停止复制并转为主实例（不会自动解决数据冲突）。

#### 原理

##### 复制初始化

1. 副本向主实例发送 `PSYNC`，尝试进行部分重同步。
2. 如果无法部分重同步，主实例生成 RDB 快照，并把期间的写命令放入复制积压缓冲区。
3. 副本加载快照，再执行快照期间缓存的写命令。

> 同步过程中副本默认仍可使用同步前的数据响应；可以使用 `replica-serve-stale-data no`（旧版本名称为 `slave-serve-stale-data`）让副本在同步完成前拒绝命令并返回 `SYNC with master in progress`。
> 断线重连时是否可以增量同步取决于复制积压缓冲区和复制 ID；无法增量同步时会退回全量同步。

##### 复制同步阶段

主数据库执行任何会导致数据变化的命令都会异步的传送给从数据库。

Redis采用乐观复制策略，容忍一定程度的主从数据库内容不同，但两者最终同步，也就是先返回结果再同步数据库，这一特性保证启用复制的主数据库性能不会受影响。

但这一特性无法感知同步了几个数据库，可以通过配置设置同步了几个从数据库，主数据库才是可写。

- `min-replicas-to-write 3`：只有至少 3 个副本满足条件时主实例才允许写入（旧配置名为 `min-slaves-to-write`）。
- `min-replicas-max-lag 10`：副本允许的最大延迟秒数（旧配置名为 `min-slaves-max-lag`）。

## 集群笔记

### 复制（replication）

Redis Cluster 在多个主节点之间分片，每个主节点负责一段 hash slot；生产环境通常还要为主节点配置副本。多键命令或 Lua 脚本要求相关 key 使用相同 hash tag（例如 `{order}:value`、`{order}:permits`），否则不能跨 slot 原子执行。

#### 创建测试集群

```bash
redis-cli --cluster create \
  127.0.0.1:7000 127.0.0.1:7001 127.0.0.1:7002 \
  127.0.0.1:7003 127.0.0.1:7004 127.0.0.1:7005 \
  --cluster-replicas 1
```

至少需要 3 个主节点才能覆盖基本的分片和故障转移场景；测试集群不能直接当作生产高可用方案。
